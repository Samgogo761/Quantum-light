# 下一阶段经典光测试路线与运行命令（2026-05-17）

本文整合两份场外 AI 意见、本地已完成测试和当前代码审查结果，给出接下来本地与服务器的实际工作安排。原则：继续冻结 BSV，只推进经典光规范、带窗和 k 网格问题。

## 1. 我对场外意见的取舍

我同意以下判断：

- 当前 RK4、Peierls Hamiltonian、Peierls velocity、电流归一化和瞬时退相干逻辑没有发现明显代码错误。
- 对称带窗的 Peierls `vg` 不收敛不是普通 bug，更像速度规范截断空间里丢失深价带导致的背景电流抵消破坏。
- 重新做 30-band Wannier90 不能自动解决 sum-rule 问题；应先在现有 112-band 模型内证明某个 active-window 路径物理上足够，再考虑重做小模型用于加速。
- 下一条最有希望的生产路径是 `lg_cov`，但必须先做全带/截断、小网格/较密网格的分层验证。

我需要修正或强调的点：

- `lg_cov 120x120 / 30 bands` 的内存不应按 12-15 GB 估算。按当前代码分配，30 bands 大约 4 GB 量级；40 bands 大约 6-7 GB；60 bands 大约 14 GB。真正要担心的是运行时间和 `zheevd` 分解开销，而不是 30/40 bands 的内存。
- `lg_cov 10x10 / 112 bands` 可以作为 smoke/benchmark，但它不是最终物理判据。协变导数有限差分需要更密 k 网格，最终至少要看 30x30/40x40。
- `matrix_vg` 去掉 `+A I` 是有用诊断，但优先级低于 `lg_cov` 路径验证。

## 2. 已经直接从现有数据回答的问题

### 2.1 Peierls full 112 的 t=0 电流

Phase 0 数据：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\runs\output_phase0_gauge_10x10_112_nodeph
```

读取 `peierls_vg/Jt.dat` 第一行：

```text
Peierls full112 J(t=0):
Jx = -2.6610658787e-7
Jy = -1.5397586808e-7
|J| = 3.0744313955e-7
```

这不是严格小于 `1e-8`，但相对本轮截断速度规范低阶伪响应已经很小。它更像 10x10 网格、有限数值精度和 Wannier 模型下的残余，而不像会解释 H1/H3 三四个数量级异常的初始化错误。

### 2.2 深价带色散

基于 Phase 0 的 `bands.dat`，10x10 网格粗估：

```text
band 1-20:  median bandwidth ≈ 0.155 eV, max ≈ 0.277 eV
band 21-50: median bandwidth ≈ 0.133 eV, max ≈ 0.192 eV
band 51-84: median bandwidth ≈ 0.184 eV, max ≈ 0.457 eV
```

说明深价带并非完全平坦。把它们在速度规范中截掉，确实可能留下明显的背景电流抵消误差。

## 3. 接下来本地机器做什么

本地只做低成本、能快速判断方向的经典光测试。

### 3.1 第一组：`lg_cov` active-window 小网格

目标：判断 `lg_cov` 是否比 Peierls 对称带窗更稳定。

脚本：

```text
C:\Users\26507\Documents\New_SBEs\Quantum-light\tools\run_local_lg_cov_tests.sh
```

建议运行顺序：

```bash
cd "/c/Users/26507/Documents/New_SBEs/Quantum-light"
export HOME=".tmp"
export TMPDIR=".tmp"
export TMP=".tmp"
export TEMP=".tmp"
export PATH=/mingw64/bin:/usr/bin:$PATH
export OMP_NUM_THREADS=8

bash tools/run_local_lg_cov_tests.sh k10_b30
bash tools/run_local_lg_cov_tests.sh k10_b40
bash tools/run_local_lg_cov_tests.sh k20_b30
bash tools/run_local_lg_cov_tests.sh k20_b40
```

可选补充：

```bash
bash tools/run_local_lg_cov_tests.sh k10_b60
bash tools/run_local_lg_cov_tests.sh k10_b112
```

说明：`k10_b112` 可能耗时较长，但它能作为 `lg_cov` 全带小网格参考。优先级低于 `k10/k20` 的 30/40 带 active-window 趋势。

输出目录：

```text
C:\Users\26507\Documents\New_SBEs\Quantum-light\local_runs\lg_cov_window_20260517_nodeph
```

### 3.2 第二组：已有 Peierls full-valence 思路先不上本地

Peierls `nb_start=1, nb_end=94/104` 保留全部价带，但内存比对称 30/40 带重很多。10x10 本地可以试，但 20x20 更适合服务器。本地优先把时间留给 `lg_cov`。

### 3.3 暂不做

- BSV；
- 120x120；
- 重新 Wannier90 30/40 bands；
- `matrix_vg` 去掉 `+A I` 的代码改动诊断。

## 4. 服务器上做什么

服务器采用 SLURM 计算节点，继续使用 `part_1` 和 `sbatch`，不要在登录节点直接长跑。

### 4.1 `lg_cov` 扫描

脚本：

```text
deploy/run_lg_cov_scan.sh
```

建议先跑小数组，确认编译和 10/20 网格：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_lg_cov_scan.sh
sbatch --array=0-4 deploy/run_lg_cov_scan.sh
```

其中：

```text
0 lgcov_k10_b112
1 lgcov_k10_b30
2 lgcov_k10_b40
3 lgcov_k20_b30
4 lgcov_k20_b40
```

如果这些通过，再跑 30x30/40x40：

```bash
sbatch --array=5-8 deploy/run_lg_cov_scan.sh
```

其中：

```text
5 lgcov_k30_b30
6 lgcov_k30_b40
7 lgcov_k40_b30
8 lgcov_k40_b40
```

如果服务器目录不同：

```bash
WORKDIR=/public/home/wangjs/project/Quantum-light \
TB_FILE=/public/home/wangjs/project/CrI3_TB/wannier/CrI3_tb.dat \
sbatch --array=5-8 deploy/run_lg_cov_scan.sh
```

输出目录默认：

```text
/public/home/wangjs/project/New_SBEs/Quantum-light/output_lg_cov_scan_nodeph
```

查看：

```bash
squeue -u wangjs
tail -f lgcov_scan_<jobid>_<arrayid>.out
tail -f output_lg_cov_scan_nodeph/lgcov_k40_b40/run.log
```

### 4.2 Peierls full-valence 扫描

目的：不是生产，而是验证“保留全部价带后低阶伪响应是否消失”。

脚本：

```text
deploy/run_peierls_fullvalence_scan.sh
```

先跑 10x10：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_peierls_fullvalence_scan.sh
sbatch --array=0-2 deploy/run_peierls_fullvalence_scan.sh
```

其中：

```text
0 vg_k10_nb94
1 vg_k10_nb104
2 vg_k10_nb112
```

如果 10x10 结果支持这个解释，再跑 20x20：

```bash
sbatch --array=3-4 deploy/run_peierls_fullvalence_scan.sh
```

其中：

```text
3 vg_k20_nb94
4 vg_k20_nb104
```

输出目录默认：

```text
/public/home/wangjs/project/New_SBEs/Quantum-light/output_peierls_fullvalence_nodeph
```

## 5. 预期判据

### 5.1 `lg_cov` 通过的条件

`lg_cov` 值得作为生产候选，如果满足：

- `k10_b30`、`k10_b40`、`k10_b60` 的谱形和关键谐波没有 Peierls 对称截断那种 H1/H3 三四个数量级异常；
- `k20_b30` 与 `k20_b40` 在 H1-H15 的趋势较稳定；
- `k30/k40` 与 `k20` 相比，高阶谱逐步稳定；
- 选择定则、偶奇阶结构、谱 envelope 没有出现明显非物理跳变。

### 5.2 Peierls full-valence 通过的条件

如果：

```text
nb_start=1, nb_end=94/104
```

在 H1/H3 上明显接近：

```text
nb_start=1, nb_end=112
```

则说明此前对称带窗问题主要就是丢失深价带的 sum-rule / 背景电流抵消。若仍然差很大，则还需要重新检查 Peierls 截断基底、U(k) 投影或电流实现。

## 6. 当前路线总结

短期：

```text
本地跑 lg_cov 10x10/20x20 的 30/40 bands。
```

中期：

```text
服务器跑 lg_cov 30x30/40x40 的 30/40 bands。
```

并行诊断：

```text
服务器跑 Peierls nb_start=1 的 full-valence 小网格，确认速度规范截断病。
```

长期：

```text
如果 active-window lg_cov 成功，再考虑重做 30/40-band Wannier90 有效模型用于加速。
```

暂不进入：

```text
BSV。
```

## 7. 服务器待办清单

### 7.1 首先上传/同步这些新文件

需要把当前仓库更新同步到服务器，至少包括：

```text
deploy/run_lg_cov_scan.sh
deploy/run_peierls_fullvalence_scan.sh
tools/run_local_lg_cov_tests.sh
docs/经典光规范与带窗测试总记录_20260517.md
docs/本地运行环境记录_20260517.md
docs/经典光后续测试路线与服务器命令_20260517.md
```

服务器上的 Wannier90 TB 文件仍使用：

```text
/public/home/wangjs/project/CrI3_TB/wannier/CrI3_tb.dat
```

### 7.2 服务器第一批：`lg_cov` 小网格确认

目的：确认服务器编译、`lg_cov` 代码路径、10x10/20x20 active-window 结果。

命令：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_lg_cov_scan.sh
sbatch --array=0-4 deploy/run_lg_cov_scan.sh
```

任务含义：

```text
0 lgcov_k10_b112
1 lgcov_k10_b30
2 lgcov_k10_b40
3 lgcov_k20_b30
4 lgcov_k20_b40
```

查看状态：

```bash
squeue -u wangjs
tail -f lgcov_scan_<jobid>_<arrayid>.out
tail -f output_lg_cov_scan_nodeph/lgcov_k20_b40/run.log
```

### 7.3 服务器第二批：`lg_cov` 较密网格

前提：第一批没有运行错误，且 10x10/20x20 结果没有明显非物理异常。

命令：

```bash
sbatch --array=5-8 deploy/run_lg_cov_scan.sh
```

任务含义：

```text
5 lgcov_k30_b30
6 lgcov_k30_b40
7 lgcov_k40_b30
8 lgcov_k40_b40
```

### 7.4 服务器并行诊断：Peierls full-valence

目的：验证 Peierls 对称带窗失败是否主要来自丢失深价带。如果 `nb_start=1` 的 94/104 带低阶接近 112 带，则支持“保留全部价带才能恢复速度规范背景抵消”的解释。

先跑 10x10：

```bash
sed -i 's/\r$//' deploy/run_peierls_fullvalence_scan.sh
sbatch --array=0-2 deploy/run_peierls_fullvalence_scan.sh
```

任务含义：

```text
0 vg_k10_nb94
1 vg_k10_nb104
2 vg_k10_nb112
```

如果 10x10 结果有诊断价值，再跑 20x20：

```bash
sbatch --array=3-4 deploy/run_peierls_fullvalence_scan.sh
```

任务含义：

```text
3 vg_k20_nb94
4 vg_k20_nb104
```

### 7.5 结果回传后需要比较的文件

每个算例至少需要：

```text
input.nml
run.log
Jt.dat
HHG.dat
Jt_decomposed.dat
```

如果要检查 valley 相关量，也带回：

```text
Jt_valley.dat
```

### 7.6 暂缓事项

在 `lg_cov` 路径和 Peierls full-valence 诊断没有完成前，服务器暂不提交：

```text
BSV jobs
120x120 production jobs
重新 Wannier90 30/40-band jobs
```

## 8. 本地 `lg_cov` 首轮结果与对服务器路线的影响

本地已完成：

```text
lg_cov k10_b30
lg_cov k10_b40
lg_cov k20_b30
lg_cov k20_b40
```

结果位置：

```text
C:\Users\26507\Documents\New_SBEs\Quantum-light\local_runs\lg_cov_window_20260517_nodeph
```

关键结论：

- `lg_cov` 没有复现 Peierls 对称截断中的巨大 H1/H3 伪响应；
- H1/H3 与 Peierls `10x10 / 112 bands` 参考处在同一量级；
- H5 以上仍有明显 k 网格和带窗依赖；
- 因此服务器 `lg_cov 30x30/40x40` 测试应作为下一步主线。

当前服务器优先级调整为：

```text
1. 先提交 deploy/run_lg_cov_scan.sh 的 array 0-4，确认服务器端结果与本地一致。
2. 若 0-4 正常，立即提交 array 5-8，测试 30x30/40x40。
3. Peierls full-valence array 0-2 可并行作为诊断，但不抢占 lg_cov 主线资源。
4. 继续冻结 BSV。
```

## 9. 服务器 `lg_cov` 扫描完成后的路线更新

服务器结果已经回传到：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_scan_nodeph
```

该目录与原 `deploy/run_lg_cov_scan.sh` 的 array 0-8 完全对应：

```text
0 lgcov_k10_b112
1 lgcov_k10_b30
2 lgcov_k10_b40
3 lgcov_k20_b30
4 lgcov_k20_b40
5 lgcov_k30_b30
6 lgcov_k30_b40
7 lgcov_k40_b30
8 lgcov_k40_b40
```

当前位置归档合理，不需要移动；后续只需要在总结文档中统一引用这个路径。

### 9.1 新结论

1. `lg_cov` 确认没有 Peierls 对称速度规范截断中的巨大低阶虚电流。
2. `40 bands` 的 30x30 到 40x40 k 收敛已经明显好转，H1-H15 大多在 10%-15% 以内，H1-H9 更稳定。
3. 30/40 bands 的带窗收敛仍不够，尤其 H7、H9、H15 差异很大。
4. 当前主问题已经不是“能不能用 `lg_cov`”，而是“`lg_cov` 需要多少 active bands 才能支撑目标谐波范围”。

### 9.2 下一轮服务器任务

我已把 `deploy/run_lg_cov_scan.sh` 扩展到 array 9-14。同步更新脚本后可运行：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_lg_cov_scan.sh
sbatch --array=9-14 deploy/run_lg_cov_scan.sh
```

新增任务：

```text
9  lgcov_k30_b50   nk=30  nb=60-109
10 lgcov_k30_b60   nk=30  nb=53-112
11 lgcov_k40_b50   nk=40  nb=60-109
12 lgcov_k40_b60   nk=40  nb=53-112
13 lgcov_k30_b112  nk=30  nb=1-112
14 lgcov_k40_b112  nk=40  nb=1-112
```

如果只想先省资源，优先跑：

```bash
sbatch --array=11-12 deploy/run_lg_cov_scan.sh
```

也就是先看 40x40 下 50/60 bands 是否能把 H7/H9/H15 拉向稳定。

### 9.3 之后如何决策

如果 `40x40 / 50/60 bands` 已经在 H1-H15 内稳定：

```text
下一步进入 T2 扫描：
T2_fs = 0.5, 1.0, 2.0, 5.337, 1.0e30
```

其中 `5.337 fs` 对应旧求解器 `deph_t2=0.5` 在 3200 nm 下的物理含义。

如果 `50/60 bands` 仍不稳定：

```text
优先比较 lgcov_k40_b112，
判断 active-window 偏差方向，
再决定是否需要 80 bands 或重新设计 active window。
```

继续暂缓：

```text
BSV
120x120 production
重新 Wannier90 30/40-band model
```

## 10. array 9-14 完成后的路线更新

新增服务器结果已经回传到：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_scan_nodeph
```

核心结论：

1. `lg_cov full112` 在 30x30 到 40x40 的 H1-H15 基本收敛。
2. 30/40/50/60 bands active window 都没有稳定贴近 full112，不能作为绝对经典光谱生产配置。
3. 当前经典光基准应切换为：

```text
gauge_method = 'lg_cov'
nkx = nky = 40
nb_start = 1
nb_end = 112
```

这不是 BSV 生产配置，而是经典光调试与退相干扫描基准。

### 10.1 立即下一步：T2 扫描

新脚本：

```text
deploy/run_lg_cov_t2_full112_scan.sh
```

服务器运行：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_lg_cov_t2_full112_scan.sh
sbatch --array=0-5 deploy/run_lg_cov_t2_full112_scan.sh
```

任务含义：

```text
0 lgcov_k40_b112_T2_0p5fs       T2_fs=0.5
1 lgcov_k40_b112_T2_1fs         T2_fs=1.0
2 lgcov_k40_b112_T2_2fs         T2_fs=2.0
3 lgcov_k40_b112_T2_5fs         T2_fs=5.0
4 lgcov_k40_b112_T2_5p337fs     T2_fs=5.337
5 lgcov_k40_b112_T2_nodeph      T2_fs=1.0e30
```

解释：

```text
T2_fs = 0.5    新求解器当前输入值，是真正 0.5 fs
T2_fs = 5.337  旧求解器 deph_t2=0.5 在 3200 nm 下的等效 fs 值
T2_fs = 5.0    文献中常见的 5 fs 量级对照
```

### 10.2 T2 扫描后如何判断

如果 `T2=5.0/5.337 fs` 与无退相干谱形接近，而 `T2=0.5 fs` 明显过度抑制高阶谐波，则后续不应再把新代码中的 `T2_fs=0.5` 当作旧求解器的复现实验。

如果 `T2=0.5 fs` 才能匹配旧结果，则需要回头检查旧求解器退相干实现是否还有额外单位约定或代码路径差异。

### 10.3 T2 之后

T2 语义锁定后，再做一个最终 k 网格检查：

```text
lg_cov / full112 / selected T2
nk = 40, 60, 80
```

若 40 与 60/80 在目标 H1-H15 内差异很小，则经典光基准可定为 40x40/full112。

继续暂缓：

```text
BSV
active-window BSV approximation
重新 Wannier90 30/40-band model
```

active-window 后续只作为便宜近似研究，不再作为当前绝对经典光谱主线。

## 11. 2026-05-18 路线修正

场外意见中有两点是重要补充：

1. `lg_cov` 的对称 active-window 截断虽然没有速度规范的灾难性伪电流，但仍然会因为丢失大量占据价带和虚跃迁通道而产生明显误差。
2. 我们还没有测试“保留全部 84 条价带，只截断高能导带”的 `lg_cov` 非对称窗口。

因此新增两个脚本：

```text
deploy/run_lg_cov_fullvalence_scan.sh
deploy/run_peierls_full112_gauge_check.sh
```

### 11.1 `lg_cov` full-valence 非对称窗口

无退相干基准：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_lg_cov_fullvalence_scan.sh
sbatch --array=0-2 deploy/run_lg_cov_fullvalence_scan.sh
```

任务：

```text
0 lgcov_k40_nb94   nk=40  nb=1-94
1 lgcov_k40_nb104  nk=40  nb=1-104
2 lgcov_k40_nb112  nk=40  nb=1-112
```

选定 T2 后也可以重跑，例如旧求解器等效退相干：

```bash
T2_FS=5.337 OUTROOT=/public/home/wangjs/project/New_SBEs/Quantum-light/output_lg_cov_fullvalence_T2_5p337fs \
  sbatch --array=0-2 deploy/run_lg_cov_fullvalence_scan.sh
```

判断标准：

```text
若 nb=1-94 或 1-104 在 H1-H15 内贴近 full112，
则它可能成为 BSV 小样本/生产的便宜候选。
否则，不再押注现有 112 带模型的简单截断。
```

### 11.2 Peierls full112 规范交叉验证

`CrI3_tb.dat` 当前为：

```text
num_wann = 112
nrpts = 579
```

Peierls full112 的 HR_proj 内存约：

```text
20x20: 46.5 GiB
30x30: 104.5 GiB
40x40: 185.8 GiB
```

因此只建议先跑 20x20：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_peierls_full112_gauge_check.sh
sbatch --array=0 deploy/run_peierls_full112_gauge_check.sh
```

任务：

```text
0 vg_k20_b112  nk=20  nb=1-112
```

若节点内存足够且 20x20 结果有价值，再考虑：

```bash
sbatch --array=1 deploy/run_peierls_full112_gauge_check.sh
```

也就是 `vg_k30_b112`。

### 11.3 关于旧 VG 对比

旧求解器 `var_method="vg"` 是 `H0 + A·P` 的 matrix-VG 形式，不是 Peierls `H(k+A)`。因此“新 Peierls VG + 外部 A(t) vs 旧 VG”不是同一规范实现，不能作为直接等价验证。

已有的旧 VG 对比应表述为：

```text
old VG vs new matrix_vg with matching/external A(t)
```

已有归档：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\runs\step1_vg_alignment
```

结果显示主电流分量相关性很高，但高阶谐波仍有差异；该结果证明新 `matrix_vg` 与旧 VG 的主时间域响应接近，却不能证明 `matrix_vg` 是强场生产路径。

### 11.4 关于 k 网格

当前 full112 的 `lg_cov` 数据已经显示：

```text
30x30 -> 40x40, H1-H15 变化约 0%-2%
```

所以 k 网格暂时不是最大风险。论文层面更合理的做法是：

```text
正文或补充材料展示 30x30、40x40、必要时 60x60 的收敛；
不必默认追求 120x120。
```

下一步若资源允许，补一个：

```text
lg_cov / full112 / selected T2 / 60x60
```

用于论文级 k 收敛确认。

## 12. 2026-05-18 追加：固定导带顶部的价带深度扫描

场外建议中“固定 `nb_end=112`，逐步补回深价带”的测试更直接地回答了当前疑问：既然 full112 的导带最高只到约 `E_F + 3.14 eV`，而对称窗口的主要差别在于丢掉大量深价带，那么应测试保留全部导带、只改变价带深度。

新增脚本：

```text
deploy/run_lg_cov_valence_depth_scan.sh
```

无退相干基准建议先跑：

```bash
cd /public/home/wangjs/project/New_SBEs/Quantum-light
sed -i 's/\r$//' deploy/run_lg_cov_valence_depth_scan.sh
sbatch --array=0-3 deploy/run_lg_cov_valence_depth_scan.sh
```

任务含义：

```text
0 lgcov_k40_nb70_112   nk=40  nb=70-112   43 bands = 15 valence + 28 conduction
1 lgcov_k40_nb60_112   nk=40  nb=60-112   53 bands = 25 valence + 28 conduction
2 lgcov_k40_nb50_112   nk=40  nb=50-112   63 bands = 35 valence + 28 conduction
3 lgcov_k40_nb30_112   nk=40  nb=30-112   83 bands = 55 valence + 28 conduction
```

`full112` 的无退相干 40x40 结果已经存在：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_scan_nodeph\lgcov_k40_b112
```

所以无退相干时可以先不重复 array 4。若后续在选定 T2 下重跑，则建议包含 array 4：

```bash
T2_FS=5.337 OUTROOT=/public/home/wangjs/project/New_SBEs/Quantum-light/output_lg_cov_valdepth_T2_5p337fs \
  sbatch --array=0-4 deploy/run_lg_cov_valence_depth_scan.sh
```

判断标准：

```text
优先比较 H1-H11。
若 nb_start=30 或 50 与 full112 在 H1-H11 内接近，例如 5%-10% 以内，
它就是比 full112 更便宜的经典光/BSV 候选。
若仍不接近，则现有 112 带模型的简单截断不适合绝对谱。
```

本地机器不建议跑这组 40x40/43-83 bands 扫描；按现有本地性能，20x20/40 bands 已接近 40 分钟量级，40x40/83 bands 会拖得很久且影响交互。服务器上单个 40x40/83 bands 预计约 60-80 分钟，更合适。

## 13. 价带深度扫描完成后的路线更新

结果位置：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_valence_depth_nodeph
```

关键结论：`nb_start=30, nb_end=112` 是目前最有希望的便宜窗口。

40x40、无退相干下，相对 full112：

```text
H1  = 0.999
H3  = 1.055
H4  = 0.955
H5  = 0.969
H6  = 0.857
H7  = 1.027
H8  = 1.186
H9  = 2.006
H11 = 2.685
```

因此：

```text
H1-H8 范围内，nb30-112 已经很接近 full112；
H9-H11 仍有明显偏差；
H11 绝对值很小，相对误差会被放大，但 H9 的 2 倍偏差仍需注意。
```

运行时间：

```text
nb30-112  约 73 min
full112   约 140 min
```

这使得 `nb30-112` 成为 BSV 小样本的现实候选，但不能直接定为最终生产配置。

### 13.1 下一步建议

等 T2 扫描结果出来后，用选定 T2 重跑 `nb30-112` 与 full112：

```bash
T2_FS=<selected> OUTROOT=/public/home/wangjs/project/New_SBEs/Quantum-light/output_lg_cov_valdepth_T2_selected \
  sbatch --array=3-4 deploy/run_lg_cov_valence_depth_scan.sh
```

如果要用旧求解器等效退相干先试：

```bash
T2_FS=5.337 OUTROOT=/public/home/wangjs/project/New_SBEs/Quantum-light/output_lg_cov_valdepth_T2_5p337fs \
  sbatch --array=3-4 deploy/run_lg_cov_valence_depth_scan.sh
```

判断：

```text
若 T2 后 nb30-112 在 H1-H8/H9 与 full112 接近，
则它可用于 BSV N=50-100 小样本。

若 H9 以上仍差异明显，
则 BSV 中关于 cutoff/平台的结论必须用 full112 或新 Wannier 小模型验证。
```

### 13.2 当前不要做

```text
不要直接把 nb30-112 用作 BSV N=500 生产；
不要回到 Peierls VG 截断；
不要用对称 30-60 bands 做绝对谱。
```
