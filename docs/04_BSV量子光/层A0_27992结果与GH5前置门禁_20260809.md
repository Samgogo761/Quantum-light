# 层 A0：27992 结果与 GH5 前置门禁（2026-08-09）

> **27992 原始计算数据有效**（6/6 COMPLETED，status=PASS）。
> 代码 WIP：`689d9a3`（含 validator_bundle 内容哈希修复）。
> **暂不提交正式 GH5**。分块 smoke compare 与 CEP 门禁未全部放行前，不生成并验收 GH5 manifests。

## 选态结论（绑定）

| 态 | 处理 |
|----|------|
| sv_r2p5_th000 | **保留** → 正式 GH5 |
| sv_r2p5_th180 | **保留** → 正式 GH5 |
| sv_r0p5_th180 | **低压缩对照**，不进正式 GH5 |

## 服务器执行状态（`689d9a3`）

| 步骤 | 状态 |
|------|------|
| 干净 clone | `/public/home/wangjs/project/New_SBEs/Quantum-light-wt-689d9a3` @ `689d9a3` |
| ifort + 单元测试 | PASS |
| 27992 audit | 三态 `status=OK`，但 CEP‑π 硬门布尔 FAIL（见下表） |
| GH3 5+4 smoke | **28861** 两片均 `COMPLETED` + `SUCCESS` + `status=PASS`（c00 ~2h34m，c01 ~2h22m） |
| merge | **PASS**（`post_28861/merge_report.json`） |
| compare vs 27992 | **仍 FAIL**（见下）；**未宣布数值等价**；**未提交 GH5**；**未重跑 smoke** |
| 正式 GH5 | **禁止**（CEP‑π 亦未放行） |

后处理产物：

- `runs/a0_full112_gh3_chunk_smoke_689d9a3/post_28861/{merge_report,compare_vs_27992}.json`
- `compare_vs_27992_semantic.json` / `compare_chunked_vs_full_semantic.log`（语义 manifest 门重跑）
- CEP 诊断：`Quantum-light-wt-689d9a3/runs/post_27992_audit/CEP_PI_DIAGNOSIS.json`

### compare 重跑（语义 manifest；不放宽复电流/连续谱门）

已确认 `290dd4a3` vs `b2b1f240` **仅为 LF/CRLF**：canonical-LF SHA 相同（1192 bytes），六列数值一致。  
`compare_chunked_vs_full.py` 已改为：

- 硬门：解析后的 `(id,w,Reα,Imα,I,φ)` 语义相等；
- 原始/`nodes_sha256`：信息性字节 provenance；报告 `canonical_lf_sha256` 与 `manifest_semantic_equal`。

对 28861 现有 `merged_plusN` 重跑结果：

| 检查 | 结果 |
|------|------|
| `manifest_semantic_equal` | **true** |
| `canonical_lf_sha_equal` | **true** |
| `raw_sha_equal` | false（预期） |
| `max_mode_rel_err` | **1.23e-4**（门 `1e-12`）→ FAIL |
| `max_ICS_rel_err` | 8.61e-8（门 `2e-7`）→ OK |
| `max_CS_rel_err` | **4.90e-7**（门 `2e-7`）→ FAIL |
| h/ω 网格舍入 | 在容差内 |

复电流/连续谱误差门**未放宽**。因此 **不能**宣布严格分块等价。

### 零成本残差诊断（不改门、不重跑）

报告：`post_28861/DIAGNOSE_chunk_vs_27992.json`

| 排行指标 | 位置 | pairwise rel | abs | \(\epsilon_{\rm abs,norm}\) | 弱信号？ |
|----------|------|-------------:|----:|------------------------------:|----------|
| max mode rel | **node5 / H9 / Jx** | 1.23e-4 | 5.35e-19 | **1.33e-16**（相对同阶 max\|J\|） | **是**（\|J\|~4e-15 vs max~4e-3） |
| max CS rel | **iw=1606**（h≈401） | 4.90e-7 | 5.92e-30 | **2.65e-24**（相对全谱 max CS） | **是**（CS~1e-23 vs max~2e-6） |

强信号归一化后，mode 最大 \(\epsilon\sim6\times10^{-15}\)，CS 最大 \(\epsilon\sim7\times10^{-10}\)。  
结论：相对 27992 为**历史回归高度一致**；当前 FAIL 混合了「分块」与「异 binary/源码」数值差，**仍不能称严格分块等价**。

### 严格分块对照（同 binary，硬绑定 28861 c00）

脚本：`deploy/a0_layerA_m88full/sbatch_full112_gh3_unchunked_samebin_control_k20.sh`

- **禁止重编译**：直接使用 c00 `run_metadata` 中的 `binary` / `binary_sha256` / `source_sha256` / `build_key`。
- 复制 c00 的 `nodes_manifest.input.dat` 与 `input.nml`；规范化 diff **仅**允许 `bsv_propagate_ids: 1..5 → 1..9`。
- OMP=36、MKL=1、`OMP_PLACES=cores`、`OMP_PROC_BIND=spread`；TB/manifest SHA 与 c00 硬相等。
- 通过门（相对 28861 `merged_plusN`）：modes \(10^{-12}\)，continuum \(2\times10^{-7}\)。
- 通过后方可宣布 GH3 5+4 分块≡未分块；**CEP‑π / GH5 仍关闭**。

## CEP‑π 定量（tol = \(10^{-3}\)；硬阶 H2/H5/H7/H9）

双方向：\(J_{+N}(\alpha)+J_{-N}(-\alpha)\) 与 \(J_{+N}(-\alpha)+J_{-N}(\alpha)\)。

| 态 | H | \(\epsilon_{P\pi}^{\max}\) | \(\epsilon_{P\pi}^{\mathrm{median}}\) | \(\mathrm{amp_{ref}}\) | 最差对 | 绝对残差 | den_local |
|----|---|-----------------------------:|----------------------------------------:|-------------------------:|--------|----------:|----------:|
| sv_r2p5_th000 | 2 | 5.09e-4 | 1.19e-8 | 8.40e-3 | 5/5 (self α=0) | 1.55e-16 | 3.03e-13 |
| sv_r2p5_th000 | 5 | **1.07e-3** | 1.79e-8 | 2.99e-2 | 5/5 (self α=0) | 5.14e-17 | 4.79e-14 |
| sv_r2p5_th000 | 7 | **1.48e-3** | 2.53e-8 | 7.22e-3 | 5/5 (self α=0) | 3.60e-17 | 2.44e-14 |
| sv_r2p5_th000 | 9 | 4.94e-4 | 3.28e-8 | 3.80e-3 | 5/5 (self α=0) | 7.29e-18 | 1.48e-14 |
| sv_r2p5_th180 | 2 | 5.09e-4 | 1.92e-8 | 9.73e-3 | 5/5 (self α=0) | 1.54e-16 | 3.03e-13 |
| sv_r2p5_th180 | 5 | **1.08e-3** | 3.21e-8 | 2.97e-2 | 5/5 (self α=0) | 5.19e-17 | 4.79e-14 |
| sv_r2p5_th180 | 7 | **1.49e-3** | 4.53e-8 | 7.14e-3 | 5/5 (self α=0) | 3.63e-17 | 2.44e-14 |
| sv_r2p5_th180 | 9 | 4.71e-4 | 5.86e-8 | 4.03e-3 | 5/5 (self α=0) | 6.95e-18 | 1.48e-14 |
| sv_r0p5_th180 | 2 | 5.08e-4 | 1.47e-8 | 1.00e-2 | 5/5 (self α=0) | 1.54e-16 | 3.03e-13 |
| sv_r0p5_th180 | 5 | **1.07e-3** | 1.83e-8 | 2.98e-2 | 5/5 (self α=0) | 5.10e-17 | 4.79e-14 |
| sv_r0p5_th180 | 7 | **1.49e-3** | 2.54e-8 | 7.31e-3 | 5/5 (self α=0) | 3.65e-17 | 2.44e-14 |
| sv_r0p5_th180 | 9 | 5.13e-4 | 3.29e-8 | 4.01e-3 | 5/5 (self α=0) | 7.57e-18 | 1.48e-14 |

粗体 = 超过 \(10^{-3}\) 硬门。所有超阈均来自 **node 5 自配对（α≈0）**；配对 ±α 的 median \(\epsilon\) 为 \(10^{-8}\) 量级。

### 失败来源诊断（优先序）

1. **#4 弱信号相对误差放大（高）** — 绝对残差 \(\sim10^{-16}\)，`den_local` \(\sim10^{-14}\)；\(\epsilon=\mathrm{abs}/\mathrm{den}\) 刚过 \(10^{-3}\)。`weak_pair` 未触发（`den_local` 仍高于 `WEAK_FRAC·amp_ref`）。
2. **#1 TB 非严格 P 共轭（低）** — 若属此类，median 与多对残差应系统性抬高；实测 median \(\sim3\times10^{-8}\)。
3. **#2 瞬时本征基 \(T_2\) 破 P 协变（低）** — 通常抬高整条 pair 分布，而非仅 α=0；**暂不优先做 \(T_2=0\) 对照**。
4. **#3 CEP/电流符号或配对约定（低）** — 翻转 −N 电流符号后，0% 节点对改善。

建议门禁修订（smoke compare 之后）：排除 `self_alpha0` 出 hard max，或对绝对残差设 floor，或对 α=0 强制用 `amp_ref` 作分母。

## 连续谱比较容差（vs 27992）

历史 `F10.4/ES16.8` vs merge `ES25.17`；按 FFT `iw` 对齐：

\[
|\Delta h|\lesssim 5.1\times 10^{-5},\qquad
|\Delta\omega|\lesssim 5.1\times 10^{-9}.
\]

物理硬相等：`TB/nodes/nk/dt/T2/r/θ/Ibar/harmonics/model`。
`source/binary/template` SHA 仅 provenance。

## 仍禁止

- 正式 GH5 / 旧 M88 GH5 / 默认 k50
- 把 CEP 布尔 FAIL 或分块 smoke 未完成当作生产放行
- 自动提交任何 GH5 任务
