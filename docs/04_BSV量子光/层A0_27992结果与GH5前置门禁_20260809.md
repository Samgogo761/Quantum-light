# 层 A0：27992 结果与 GH5 前置门禁（2026-08-09）

> **27992 原始计算数据有效**（6/6 COMPLETED，status=PASS）。
> **当前代码仍非生产版** — d578ac8 / eb307f6 及后续 follow-up 仅为非生产 WIP；
> **暂不提交正式 GH5**。smoke 全 PASS 前不生成并验收 GH5 manifests。

## 选态结论（绑定）

| 态 | 处理 |
|----|------|
| sv_r2p5_th000 | **保留** → 正式 GH5 |
| sv_r2p5_th180 | **保留** → 正式 GH5 |
| sv_r0p5_th180 | **低压缩对照**，不进正式 GH5 |

## 硬阻塞修复状态

| # | 项 | 状态 |
|---|-----|------|
| 1 | propagate_ids 真负测 + 纯数字 token | 已修 |
| 2 | merge 权重和容差 = Fortran 5e-8（不重生成 27992 manifest） | 已修 |
| 3 | smoke 共享 OUTROOT = SLURM_ARRAY_JOB_ID | 已修 |
| 4 | merged 必出 manifest/provenance；compare 禁 fallback | 已修 |
| 5 | docs UTF-8 / 控制字符清理 | 本轮 |
| 6 | 目标阶节点 ICS/CS ↔ 连续谱 FFT bin 交叉验证 | 已修 |
| 7 | 连续谱按 FFT `iw` 对齐；允许 F10.4/ES16.8 舍入 | 本轮 |
| 8 | Linux parser 测试路径 `.resolve()` | 本轮 |
| 9 | `template`/source/binary SHA 仅作实现 provenance，不强制=27992 | 本轮 |
| 10 | SUCCESS 复用核 `propagate_ids`/`chunk_index`/validator/输出哈希 | 本轮 |
| 11 | merge+compare 正向测试（不能仅靠负向“碰巧通过”） | 本轮 |

## CEP-π 门 — PENDING（服务器重跑）

双方向：

\[
J_{+N}(\alpha)+J_{-N}(-\alpha),\qquad J_{+N}(-\alpha)+J_{-N}(\alpha)
\]

弱节点用谐波系综 `amp_ref` 归一。任一态 INCOMPLETE/FAIL → exit ≠ 0。
Jo 连续谱 SNR：暂不可得。

## 连续谱比较容差（vs 27992）

历史作业 `HHG_ics_cs.dat` 用 `F10.4/ES16.8`；分块 merge 用 `ES25.17`。
按 FFT 行号 `iw` 对齐后允许：

\[
|\Delta h|\lesssim 5.1\times 10^{-5},\qquad
|\Delta\omega|\lesssim 5.1\times 10^{-9}.
\]

物理硬相等：`TB/nodes/nk/dt/T2/r/θ/Ibar/harmonics/model`。
`source/binary/template` SHA 只记录，不强制与 27992 相等；解析后的物理输入必须相等。

## 真实分块 smoke（勿与文本切块混淆）

推送分支后，在服务器创建干净 worktree，并显式传入 `REPO=<新worktree>`：

```bash
sbatch deploy/a0_layerA_m88full/sbatch_full112_gh3_chunk_smoke_5plus4.sh
python tools/analysis/merge_chunked_ensemble.py ...
python tools/analysis/compare_chunked_vs_full.py \
  --merged .../merged_plusN \
  --full-run-dir .../sv_r2p5_th180_plusN
```

顺序：ifort → 27992 audit → GH3 5+4 smoke → strict merge → 连续谱比较。

## 仍禁止

- 旧 M88 GH5 脚本；正式 M88 GH5 / k50
- `git add .`；在 smoke/CEP/provenance 全 PASS 前当作生产
- 正式 GH5（还缺 GH5 manifests/generator；smoke 未全 PASS 前不要提交）

## 正确执行顺序

1. 本轮 follow-up：网格容差、parser `.resolve()`、文档控制字符、physics/build 分流、SUCCESS 复用、正向 merge+compare
2. 推送分支；服务器干净 worktree + `REPO=...`
3. 27992 audit + 真实 GH3 5+4 smoke 全 PASS
4. 全部 PASS 后才生成并验收两态 full112 GH5@k20 manifests
