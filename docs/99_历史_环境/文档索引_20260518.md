# 文档索引（2026-05-18）

这个目录里的 md 文件现在按中文命名。建议优先阅读或发送顺序如下。

## 1. 最推荐发给外部 AI

```text
给外部AI的当前状态总结_20260518.md
```

用途：最适合直接复制给外部 AI。它包含当前最新结论、关键数据、要问外部 AI 的问题和推荐附图。

## 2. 关键数据补充

```text
全价带导带截断收敛测试_20260518.md
```

用途：记录 `nb1-94 / nb1-104 / full112` 的直接对比。  
这是目前判断“保留全部价带、只截高导带”是否可用的核心文件。

```text
k网格与退相干输入更新_20260518.md
```

用途：记录 `full112` 的 k 网格谱图、`30x30 -> 40x40` 收敛，以及新代码 `T2_cycles` 的退相干输入语义。

```text
Wannier模型与截断决策总结_20260518.md
```

用途：回答“为什么不立刻重做少带 Wannier”“112-band 模型是不是必要”“截断为什么要保留全部价带”等问题。

## 3. 历史过程记录

```text
经典光规范与带窗测试总记录_20260517.md
```

用途：长篇全过程记录，包括 Phase 0、Peierls 失败、早期 lg_cov 扫描、场外 AI 问答等。  
不建议第一时间发给外部 AI，太长，而且早期结论部分已经被后续 `nb1-104` 结果更新。

```text
经典光后续测试路线与服务器命令_20260517.md
```

用途：记录服务器 sbatch 路线和当时的执行计划。  
如果外部 AI 要检查“下一步服务器怎么跑”，可以发这一份；否则不必优先发。

## 4. 代码与环境背景

```text
SBE求解器理论与验证记录.md
```

用途：代码理论、旧求解器对比、SBE 方程路径、BSV 路径状态。  
适合给需要审代码逻辑的人，不适合作为现状摘要。

```text
本地运行环境记录_20260517.md
```

用途：本地 Windows/MSYS2/gfortran 编译运行遇到的问题和解决方案。  
除非讨论本地复现，不需要发给外部 AI。

```text
BSV光集成指南.md
```

用途：BSV 代码集成和工程说明。  
当前 BSV 仍冻结，因此不是现阶段讨论重点。

## 5. 图表目录

```text
docs/figures/classical_validation
```

最重要的图：

```text
fig09_hhg_spectrum_lgcov_full112_kgrid.svg
fig10_hhg_spectrum_ratio_lgcov_full112_kgrid.svg
fig11_hhg_spectrum_lgcov_fullvalence.svg
fig12_even_harmonic_ratios_lgcov_windows.svg
```

对应含义：

```text
fig09/fig10: full112 的 k 网格收敛
fig11/fig12: nb1-94 / nb1-104 / full112 的带窗收敛，尤其偶次谐波
```

## 6. 最简发送建议

如果只发一个文件：

```text
给外部AI的当前状态总结_20260518.md
```

如果发一个文件加图：

```text
给外部AI的当前状态总结_20260518.md
fig09_hhg_spectrum_lgcov_full112_kgrid.svg
fig10_hhg_spectrum_ratio_lgcov_full112_kgrid.svg
fig11_hhg_spectrum_lgcov_fullvalence.svg
fig12_even_harmonic_ratios_lgcov_windows.svg
```

如果外部 AI 质疑 Wannier 或截断逻辑，再补：

```text
Wannier模型与截断决策总结_20260518.md
全价带导带截断收敛测试_20260518.md
```
