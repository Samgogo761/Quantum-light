# 给外部 AI 的当前状态总结（2026-05-19）

这份文件是目前最适合直接发给外部 AI 的精简版状态记录。重点更新是：`T2_cycles=0.5` 下的 `nb1-104` vs full112 已完成。

## 1. 当前生产路线判断

经典光主路线：

```text
gauge_method = lg_cov
nkx = nky = 40
classical absolute benchmark = full112
economic candidate = nb_start=1, nb_end=104
```

暂不进入 BSV 大样本生产。BSV 需要等经典光的 T2、带窗和 k 网格最终确认后再做 pilot。

## 2. 退相干参数语义

旧求解器输入：

```text
deph_t2 = 0.5
```

不是 `0.5 fs`，而是 `0.5 optical cycle`。对 3200 nm：

```text
T_cycle ≈ 10.674 fs
0.5 cycle ≈ 5.337 fs
```

新求解器已支持：

```fortran
&dephasing
  T2_cycles = 0.5
/
```

当 `T2_cycles > 0` 时，它覆盖 `T2_fs`。

## 3. 最新服务器结果：T2=0.5 cycle

数据位置：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_fullvalence_T2_0p5cycle
```

服务器日志确认：

```text
T2 input     :     0.5000 optical cycles
T2 effective :   5.3370E+00 fs
```

初始电流：

```text
|J(0)| nb1-104 = 4.4081e-18
|J(0)| full112 = 4.4046e-18
```

时间域电流一致性：

```text
corr_x = 0.999954294
corr_y = 0.999657378
RMS diff x / RMS full112 x = 0.0215
RMS diff y / RMS full112 y = 0.0238
```

## 4. T2=0.5 cycle 下 nb1-104/full112 谐波比值

| harmonic | ratio |
|---:|---:|
| H1 | 0.961 |
| H2 | 0.977 |
| H3 | 0.999 |
| H4 | 0.998 |
| H5 | 1.001 |
| H6 | 1.019 |
| H7 | 0.994 |
| H8 | 1.017 |
| H9 | 0.975 |
| H10 | 1.341 |
| H11 | 1.110 |

判断：

```text
H1-H9：nb1-104 与 full112 基本一致。
H2/H4/H6/H8：偶次谐波误差约 2.3% 以内。
H10-H11：绝对强度已经很低，比例更敏感。
H12 以后：接近数值地板，不作为当前截断路线主判据。
```

## 5. 与无退相干的关系

无退相干下，`nb1-104/full112` 对 H1-H11 也已经很好：

```text
H1  0.958
H2  1.015
H3  0.998
H4  0.998
H5  1.008
H6  0.998
H7  1.001
H8  1.013
H9  0.973
H10 0.974
H11 0.986
```

因此“全价带 + 截高导带”不是只在无退相干下偶然有效；在旧求解器等价 T2 下仍然有效。

## 6. 当前理论判断

1. Peierls VG 的简单截断不适合作为生产路线，原因是截价带会破坏速度规范中的基态电流抵消。
2. `lg_cov` 的截断误差更温和，表现为虚跃迁完备性误差，而不是低阶伪背景电流。
3. 只有“保留全部 84 条价带、只截去高导带”的截断方式目前被数据支持。
4. `nb1-104` 是当前最好的经济候选；`full112` 仍是经典光绝对基准。
5. 不建议现在重做少带 Wannier90。当前 112-band 模型对应双层 CrI3 的 Cr-d/I-p spinor 化学完备基。少带 Wannier 是新的有效模型，必须重新对 full112 验证，不能自动解决规范和截断问题。

## 7. 图像

已有图像目录：

```text
docs/figures/classical_validation
```

最新相关图：

```text
fig13_hhg_spectrum_lgcov_fullvalence_T2_0p5cycle.svg
fig14_hhg_ratio_lgcov_nb104_full112_T2_0p5cycle.svg
fig15_hhg_spectrum_lgcov_full112_k40_k60_T2_0p5cycle.svg
fig16_hhg_ratio_lgcov_full112_k60_k40_T2_0p5cycle.svg
fig17_hhg_spectrum_lgcov_full112_T2_scan.svg
fig18_hhg_ratio_lgcov_full112_T2_to_nodeph.svg
fig19_even_harmonics_lgcov_full112_T2_scan.svg
fig20_hhg_spectrum_lgcov_band_economy_T2_0p5cycle.svg
fig21_hhg_ratio_lgcov_band_economy_T2_0p5cycle.svg
fig22_initial_current_lgcov_band_economy_T2_0p5cycle.svg
```

以前的关键图：

```text
fig09_hhg_spectrum_lgcov_full112_kgrid.svg
fig10_hhg_spectrum_ratio_lgcov_full112_kgrid.svg
fig11_hhg_spectrum_lgcov_fullvalence.svg
fig12_even_harmonic_ratios_lgcov_windows.svg
```

## 8. k 网格最新确认：60x60 full112 / T2=0.5 cycle

数据位置：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_k60_b112_T2_0p5cycle
```

与 `40x40 full112 / T2=0.5 cycle` 比较：

```text
H1  1.0001
H2  0.9997
H3  1.0001
H4  1.0001
H5  1.0004
H6  1.0055
H7  1.0038
H8  1.0119
H9  1.0013
H10 0.9924
H11 1.0136
```

时间域电流：

```text
corr_x = 0.999999995
corr_y = 0.999991622
```

结论：

```text
lg_cov / full112 / T2=0.5 cycle 下，40x40 -> 60x60 已经收敛。
H1-H11 最大偏差约 1.36%，低阶偶次 H2/H4/H6/H8/H10 在约 1.2% 以内。
40x40 可以作为经典光论文级基准，60x60 可作为收敛确认点。
```

## 9. T2 敏感性扫描最新结果

数据位置：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_t2_key_cases
```

本次完成：

```text
T2 = 0.5 fs, 1 fs, 2 fs, 5 fs
```

并与已有 `T2=0.5 cycle≈5.337 fs`、`nodeph` 比较。

相对无退相干的总谐波比值：

```text
          0.5fs    1fs     2fs     5fs     0.5cycle
H1        3.0945   2.2905  1.5673  1.1412  1.1270
H2        4.0491   1.5555  0.8739  0.6110  0.6133
H3        0.0019   0.0353  0.1517  0.4273  0.4489
H4        0.0003   0.0013  0.0032  0.0579  0.0676
H5        0.0019   0.0211  0.0962  0.2825  0.3003
H6        0.0000   0.0005  0.0037  0.0341  0.0391
H8        0.0000   0.0000  0.0001  0.0103  0.0132
H10       0.0000   0.0000  0.0002  0.0024  0.0030
H11       0.0001   0.0000  0.0083  0.3473  0.4059
```

结论：

```text
T2 是强敏感参数。
旧求解器 deph_t2=0.5 应对应 T2_cycles=0.5≈5.337 fs，而不是 0.5 fs。
T2=0.5 fs 会把 H3 以上谐波压低几个数量级，不能与旧代码 0.5 参数混用。
T2=5 fs 与 0.5 cycle 定性接近，但高阶仍有 10%-25% 量级差异。
论文中建议给出 T2 敏感性，而不是只报单一 T2。
```

## 10. 带窗经济配置最新结果

新增数据：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_fullvalence_T2_0p5cycle
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_lg_cov_valence_trim_104_T2_0p5cycle
```

比较对象：

```text
full112
nb1-104
nb1-94
nb10-104
nb20-104
```

初始电流：

```text
full112   4.4046e-18
nb1-104   4.4081e-18
nb1-94    4.4117e-18
nb10-104  1.0983e-07
nb20-104  1.9860e-08
```

相对 full112 的 H1-H11 比值：

```text
          nb1-94  nb1-104  nb10-104  nb20-104
H1        0.8230  0.9610   0.9313    0.9184
H2        0.9307  0.9775   1.3476    0.7748
H3        1.0115  0.9986   1.0150    1.0586
H4        1.0084  0.9982   1.0441    0.9200
H5        0.9840  1.0009   1.1890    1.0987
H6        0.9843  1.0194   1.1109    1.0023
H7        0.9550  0.9943   0.9362    0.9940
H8        0.9718  1.0167   1.1622    1.3827
H9        1.1046  0.9755   0.9135    0.9849
H10       2.0076  1.3409   2.6908    2.9651
H11       0.9359  1.1098   1.0438    0.3618
```

结论：

```text
nb1-104 仍是最稳经济配置。
nb1-94 保留全部价带、初始电流干净，但 H1 低约 18%，H2 低约 7%；可作为更便宜探索候选。
nb10-104 / nb20-104 截掉价带，初始电流不为零，偶次谐波和 Jy 时间域响应偏差明显，不建议继续推进。
```

## 11. 下一步建议

优先级：

```text
1. 经典光测试阶段可以收束：full112 为基准，nb1-104 为经济候选，nb1-94 为便宜探索候选。
2. BSV 前先改代码输出样本方差/误差条。
3. BSV 先做 N=50-100 pilot，同时比较 30x30 和 40x40 的系综平均谱。
4. pilot 优先使用 nb1-104；如需更便宜试探，可加 nb1-94 对照。
```

暂不建议继续跑：

```text
nb10-104 / nb20-104 的 50x50 扩展
Peierls VG 截断生产
BSV 500 大样本
```

## 12. 新增 AFM Néel 选择定则检查

下一项建议加入的物理 debug 是比较两套相反反铁磁序参量 TB 数据：

```text
N       : AFM Néel vector = N
-N      : AFM Néel vector = -N
```

理想选择定则：

```text
J_n(-N) = (-1)^(n+1) J_n(N)
```

因此：

```text
奇次谐波主要在 evenN = [J(N)+J(-N)]/2 通道
偶次谐波主要在 oddN  = [J(N)-J(-N)]/2 通道
```

注意这必须在 `Jt.dat` 的复数 Fourier 电流振幅上做，不能直接对 `HHG.dat` 的强度平方相加相减。

已新增说明与工具：

```text
docs/AFM_Neel选择定则检查方案_20260526.md
tools/analysis/afm_neel_symmetry_check.py
```

这项检查需要真正的两套 `N/-N` Wannier TB 文件。不能只改 `input.nml` 里的 `SOC` 参数，因为当前磁序和 SOC 信息都已经编码在 `wannier_tb_file` 中。

## 13. 弱场 scaling 测试最新结果

数据位置：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_weakfield
```

配置：

```text
lg_cov / 40x40 / nb1-104 / T2_cycles=0.5
I = 2.0e11, 5.0e10, 1.25e10 W/cm^2
```

结论：

```text
H1 线性响应通过：HHG 强度指数 p≈2，直接电流振幅指数 p≈1。
H2 大体接近二阶，但仍有偏差。
H3 尚未进入理想三阶微扰区。
```

新增 TB 质量诊断：

```text
H(k) Hermiticity max = 8.11e-16
r(k) Hermiticity max = 7.31e-03
v(k) Hermiticity max = 9.87e-04
```

`H(k)` 很干净；`r(k)`/`v(k)` 的当前诊断是绝对最大元，且代码实际使用前会 Hermitize，但这仍提示需要继续做按分量、相对 Frobenius 误差的矩阵元质量审计。

建议下一轮用更低基准强度验证真正弱场极限：

```bash
WEAKFIELD_INTENSITY_FULL=2.0e10 \
WEAKFIELD_OUTROOT=/public/home/wangjs/project/New_SBEs/Quantum-light/output_weakfield_I2e10 \
sbatch deploy/run_weakfield_scaling.sh
```

## 14. 弱场 scaling 更新：I_full = 2.0e10 W/cm^2

数据位置：

```text
C:\Users\26507\Documents\量子光研究\新SBEs\data\output_weakfield_I2e10
```

配置：

```text
lg_cov / 40x40 / nb1-104 / T2_cycles=0.5
I = 2.0e10, 5.0e9, 1.25e9 W/cm^2
```

`HHG.dat` 强度指数：

```text
E0 -> E0/2:
  H1 p_yield = 2.0106
  H2 p_yield = 5.1962
  H3 p_yield = 5.4852

E0/2 -> E0/4:
  H1 p_yield = 2.0033
  H2 p_yield = 4.6586
  H3 p_yield = 5.8445
```

对应电流振幅指数 `p_amp = p_yield/2`：

```text
E0 -> E0/2:
  H1 p_amp = 1.0053
  H2 p_amp = 2.5981
  H3 p_amp = 2.7426

E0/2 -> E0/4:
  H1 p_amp = 1.0017
  H2 p_amp = 2.3293
  H3 p_amp = 2.9222
```

从 `Jt.dat` 直接做整数频率投影得到的电流振幅指数：

```text
E0 -> E0/2:
  H1 1.01, H2 2.11, H3 2.74, H4 3.54, H5 4.54, H6 5.09

E0/2 -> E0/4:
  H1 1.00, H2 2.05, H3 2.92, H4 3.55, H5 3.92, H6 2.21

E0 -> E0/4:
  H1 1.00, H2 2.08, H3 2.83, H4 3.55, H5 4.23, H6 3.65
```

判断：

```text
H1 线性响应完全通过。
H2 在直接电流投影中基本满足二阶响应。
H3 已接近三阶响应，尤其 E0/2 -> E0/4 为 2.92。
HHG.dat 峰值法对 H2 的弱峰更敏感，因此比直接电流投影更保守。
```

逐分量 TB 质量诊断：

```text
H(k)   max|H-H+| = 8.11e-16, avg rel_frob = 6.33e-16
r_x(k) max|r-r+| = 7.31e-03, avg rel_frob = 1.02e-03
r_y(k) max|r-r+| = 6.61e-03, avg rel_frob = 9.88e-04
r_z(k) max|r-r+| = 2.12e-11, avg rel_frob = 3.69e-13
v_x(k) max|v-v+| = 9.87e-04, avg rel_frob = 3.29e-03
v_y(k) max|v-v+| = 9.40e-04, avg rel_frob = 3.14e-03
v_z(k) max|v-v+| = 3.31e-12, avg rel_frob = 7.17e-13
```

初始电流：

```text
E0    : |J(0)| = 4.3862e-18
E0/2  : |J(0)| = 4.4265e-18
E0/4  : |J(0)| = 4.3935e-18
```

当前结论：

```text
弱场 scaling 审查基本通过，尤其 H1/H2/H3 的直接电流投影没有暴露出系统性代码错误。
暂时不必继续跑 I_full=2e9，除非需要非常严格展示 HHG.dat 峰值指数的微扰极限。
下一项最高优先级仍是 AFM N/-N 选择定则检查，需要真正的 -N QE+Wannier TB。
```
